function allIndexesOf(array, value){
    let indexes = [];
    if(value === ''){
        return indexes;
    } else {
    }
}

let fruits = ['apple', 'banana', 'orange', 'apple', 'pineapple'];
console.log(allIndexesOf(fruits, ''));